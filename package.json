{
  "name": "hexo-generator-readtime",
  "version": "1.0.4",
  "description": "Generates a word counting, character counting, images counting and read time counting, then updates the front-matter of the posts with the correct values.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  }
}
